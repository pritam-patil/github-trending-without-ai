[project]
name = "github-trending-without-ai"
version = "0.1.0"
description = "GitHub trending, with the AI/LLM repos filtered out — auto-updated daily"
readme = "README.md"
authors = [
    { name = "Pritam Patil", email = "pritam18@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
]

[tool.uv]
package = false
