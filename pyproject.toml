[project]
name = "github-trending-without-ai"
version = "0.1.0"
description = "A non-AI view of GitHub Trending — discover open-source tools, libraries, languages, and projects trending right now."
readme = "README.md"
authors = [
    { name = "Pritam Patil", email = "pritam18@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.uv]
package = false
